export DATABASE_URL
export JWT_SECRET
export REDIS_ADDR
export SMTP_HOST
export SMTP_PORT
export SMTP_USERNAME
export SMTP_PASSWORD
export SMTP_SENDER
export METRICS_USERNAME
export METRICS_PASSWORD
# Set to "true" to log full password reset tokens (development only, never in production!)
export LOG_TOKENS=false
# Set to "true" if running behind a reverse proxy (nginx, AWS ALB, Cloudflare, etc.)
# This enables X-Forwarded-For header parsing for accurate client IP logging and rate limiting
export TRUST_PROXY=false