banking-app/
│
├── cmd/
│   └── bank/
│       └── main.go                # ✅ Entry point — starts your CLI or HTTP server
│
├── internal/
│   ├── api/
│   │   ├── handlers.go            # ✅ REST endpoints: accounts, transactions, transfers
│   │   ├── middleware.go          # ✅ Logging, auth, context cancellation
│   │   └── router.go              # ✅ Setup routes, mux, etc.
│   │
│   ├── core/
│   │   ├── account.go             # ✅ Account struct + methods (Deposit, Withdraw)
│   │   ├── transaction.go         # ✅ Transaction struct + logic
│   │   ├── transfer.go            # ✅ Transfer logic (atomic)
│   │   └── errors.go              # ✅ Custom errors for validation, overdraft, etc.
│   │
│   ├── storage/
│   │   ├── memory/
│   │   │   └── memory_store.go    # ✅ In-memory store (used in Week 1–3)
│   │   ├── file/
│   │   │   └── file_store.go      # ✅ JSON storage (Week 3)
│   │   ├── postgres/
│   │   │   ├── db.go              # ✅ PostgreSQL connection setup (Week 8)
│   │   │   ├── account_repo.go    # ✅ CRUD ops for accounts
│   │   │   ├── transaction_repo.go# ✅ CRUD ops for transactions
│   │   │   └── transfer_repo.go   # ✅ Transaction-safe transfers
│   │   └── storage.go             # ✅ `Storage` interface (Week 3)
│   │
│   └── config/
│       ├── config.go              # ✅ Load DB credentials, app env vars
│       └── .env.example           # ✅ Example environment file
│
├── pkg/
│   ├── logger/
│   │   └── logger.go              # ✅ Simple logger abstraction
│   ├── utils/
│   │   └── id.go                  # ✅ UUID or nanoid generator
│   └── test/
│       └── test_helpers.go        # ✅ DB test utilities, rollback helpers
│
├── migrations/
│   └── 001_init.sql               # ✅ Create users, accounts, transactions tables (Week 8)
│
├── go.mod
├── go.sum
└── README.md
